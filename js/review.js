// empty object
var userName = prompt("Enter Name");

if (userName.valueOf.length == 0 || userName == ' ' || userName != NaN){
    alert('Please enter a name');
}else { if(userName.valueOf.length > 0 && userName != ' ' && userName == NaN){
    console.log(userName);
}
}